import { createTheme } from "@mui/system"

const theme = createTheme({
  palette: {
    primary: {
      // light: '#757ce8',
      main: '###FFB966',
      // dark: '#002884',
      // contrastText: '#fff',
    },
    secondary: {
      // light: '#ff7961',
      main: '##FADC69',
      // dark: '#ba000d',
      // contrastText: '#000',
    },
  },
});

export default theme;
